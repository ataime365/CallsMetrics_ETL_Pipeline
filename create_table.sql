CREATE TABLE benson_schema.callsx (
    id INTEGER PRIMARY KEY,
    sid VARCHAR(255),
    account_id INTEGER,
    name VARCHAR(255),
    cnam VARCHAR(255),
    search VARCHAR(255),
    referrer VARCHAR(255),
    location VARCHAR(255),
    source VARCHAR(255),
    source_id INTEGER,
    source_sid VARCHAR(255),
    tgid INTEGER,
    likelihood NUMERIC,
    duration INTEGER,
    direction VARCHAR(255),
    talk_time INTEGER,
    ring_time INTEGER,
    hold_time INTEGER,
    wait_time INTEGER,
    parent_id NUMERIC,
    email VARCHAR(255),
    street VARCHAR(255),
    city VARCHAR(255),
    state VARCHAR(255),
    country VARCHAR(255),
    postal_code VARCHAR(255),
    called_at TIMESTAMP WITH TIME ZONE,
    unix_time INTEGER,
    tracking_number_id INTEGER,
    tracking_number_sid VARCHAR(255),
    tracking_number VARCHAR(255),
    tracking_label VARCHAR(255),
    dial_status VARCHAR(255),
    is_new_caller BOOLEAN,
    indexed_at NUMERIC,
    inbound_rate_center_country VARCHAR(255),
    inbound_rate_center_prefix VARCHAR(50),
    inbound_rate_center_tollfree BOOLEAN,
    billed_amount NUMERIC,
    billed_at TIMESTAMP WITH TIME ZONE,
    caller_number_split VARCHAR(255),
    contact_number VARCHAR(255),
    excluded BOOLEAN,
    redacted BOOLEAN,
    tracking_number_format VARCHAR(255),
    caller_number_format VARCHAR(255),
    alternative_number VARCHAR(255),
    caller_number_complete VARCHAR(255),
    caller_number_bare VARCHAR(255),
    tracking_number_bare VARCHAR(255),
    caller_number VARCHAR(255),
    visitor BOOLEAN,
    call_path_route_name VARCHAR(255),
    call_path_route_id VARCHAR(255),
    call_path_route_type VARCHAR(255),
    call_path_started_at TIMESTAMP WITH TIME ZONE,
    left_talk_time INTEGER,
    right_talk_time INTEGER,
    transfers VARCHAR(255),
    call_status VARCHAR(255),
    status VARCHAR(255),
    spotted VARCHAR(255),
    salesforce VARCHAR(255),
    audio VARCHAR(255),
    callbacks VARCHAR(255),
    emails VARCHAR(255),
    day VARCHAR(255),
    month VARCHAR(255),
    hour VARCHAR(255),
    ga_cid VARCHAR(255),
    tag_list VARCHAR(255),
    notes TEXT,
    latitude NUMERIC,
    longitude NUMERIC,
    extended_lookup_on BOOLEAN,
    agent_id VARCHAR(255),
    agent VARCHAR(255),
    legs TEXT
);
